pull_request_rules:
  - name: Automatic merge
    description: automatic merge when CI passes and review is done
    conditions:
      - '#approved-reviews-by>=1'
      - status-success=CI
    actions:
      request_reviews:
        users:
          - honhimW
